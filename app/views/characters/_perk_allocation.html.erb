<div class="nl-allocation-panel"
     data-controller="perk-allocation"
     data-perk-allocation-free-value="<%= @perk_points %>"
     data-perk-allocation-yes-label-value="<%= t("game.profile.perk_yes") %>"
     data-perk-allocation-no-label-value="<%= t("game.profile.perk_no") %>">
  <div class="nl-allocation-points">
    <span class="nl-allocation-points-label"><%= t("game.profile.possible_perks") %>:</span>
    <span class="nl-allocation-points-value" data-perk-allocation-target="freePoints"><%= @perk_points %></span>
  </div>

  <%= form_with url: perks_character_path(@character), method: :patch, class: "nl-allocation-form", data: {turbo_frame: "perk-allocation"} do %>
    <div class="nl-skills-grid">
      <% @perk_definitions.each do |perk_key, definition| %>
        <% owned = @character.owns_perk?(perk_key) %>
        <div class="nl-skill-row nl-perk-row" data-perk="<%= perk_key %>">
          <div class="nl-skill-info">
            <span class="nl-skill-name"><%= definition[:name] %></span>
            <p class="nl-skill-description"><%= definition[:description] %></p>
          </div>

          <div class="nl-skill-controls">
            <% if !owned && @perk_points.positive? %>
              <button type="button"
                      class="nl-stat-btn nl-stat-btn--minus"
                      aria-label="<%= t("game.profile.perk_remove_aria", name: definition[:name]) %>"
                      data-action="click->perk-allocation#removePerk"
                      data-perk-allocation-perk-param="<%= perk_key %>">
                -
              </button>
            <% end %>

            <span class="nl-perk-state<%= " nl-perk-state--owned" if owned %>"
                  data-perk-allocation-target="perkState"
                  data-perk="<%= perk_key %>"
                  data-owned="<%= owned %>">
              <%= owned ? t("game.profile.perk_yes") : t("game.profile.perk_no") %>
            </span>

            <% if !owned && @perk_points.positive? %>
              <button type="button"
                      class="nl-stat-btn nl-stat-btn--plus"
                      aria-label="<%= t("game.profile.perk_add_aria", name: definition[:name]) %>"
                      data-action="click->perk-allocation#addPerk"
                      data-perk-allocation-perk-param="<%= perk_key %>">
                +
              </button>

              <%= hidden_field_tag "selected_perks[#{perk_key}]", 0,
                                   data: {perk_allocation_target: "perkInput", perk: perk_key} %>
            <% end %>
          </div>

          <div class="nl-skill-meta">
            <span class="nl-perk-source-id"><%= t("game.profile.perk_source", id: definition[:source_id]) %></span>
          </div>
        </div>
      <% end %>
    </div>

    <% if @perk_points.positive? %>
      <div class="nl-allocation-actions">
        <button type="button"
                class="nl-btn"
                data-action="click->perk-allocation#reset">
          <%= t("game.profile.perk_reset") %>
        </button>
        <button type="submit"
                class="nl-btn nl-btn--disabled"
                data-perk-allocation-target="saveButton"
                disabled>
          <%= t("game.profile.perk_save") %>
        </button>
      </div>
    <% else %>
      <p class="nl-perk-empty-points"><%= t("game.profile.no_perk_points") %></p>
    <% end %>
  <% end %>
</div>
