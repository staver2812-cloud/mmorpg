<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <title><%= content_for(:title) || t("app.title") %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="theme-color" content="#1a1410">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "application", "controls", data: {turbo_track: "reload"} %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="nl-public-layout<%= " #{content_for(:body_class)}" if content_for?(:body_class) %>">
    <div class="nl-public-topbar">
      <div class="nl-public-brand"><%= t("app.title") %></div>
      <%= render "shared/locale_switcher" %>
    </div>
    <div id="flash" class="nl-public-flash">
      <% flash.each do |type, message| %>
        <%= render "shared/flash", type:, message: %>
      <% end %>
    </div>
    <main class="nl-public-main" id="game-main">
      <%= yield %>
    </main>
  </body>
</html>
